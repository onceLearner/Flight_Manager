import javax.swing.*;

public class GestionAdmin extends JFrame {

    /**
     *
     * @param tab number of the current tab so we know which tables to hit in the db
     */
    public GestionAdmin(int tab){
        switch (tab) {
            case 0:

        }

    }

}
